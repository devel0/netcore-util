# CommandParser property (SearchAThing.CmdlineParseItem)
if this item is a command then this sub parser allow to go deep of another level
            (see examples/cmdline-parser-04)

## Signature
```csharp
public CmdlineParser CommandParser
{
    get;
    protected set;
}
```
