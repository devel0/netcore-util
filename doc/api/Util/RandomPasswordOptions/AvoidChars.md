# AvoidChars property (SearchAThing.Util.RandomPasswordOptions)
avoid ambigous chars
            for example : new[] { 'l', 'I', 'O', '0' }
            default: null

## Signature
```csharp
public char[] AvoidChars
{
    get;
    set;
}
```
